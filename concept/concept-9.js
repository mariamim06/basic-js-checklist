// 9. use any for loop to display every elements of an array

var numbers = [23, 04, 6, 30, 12, 60, 44, 9];
for(i=0; i< numbers.length; i++) {
    var element = numbers[i];
    console.log(element);
}